<div class="chatbox mt-3">
  <div id="messages" data-chatroom-id="<%= project.id %>">
    <% project.messages.each do |message| %>
      <div class="message-container" id="message-<%= message.id %>">
        <i class="author">
          <span><%= message.user.username %></span>
          <small><%= message.created_at.strftime("%a %b %e at %l:%M%p") %></small>
        </i>
        <p><%= message.content %></p>
      </div>
    <% end %>
  </div>
  <div>
    <% if user_project.id %>
      <%= simple_form_for [ project, message ], remote: true do |f| %>
        <%= f.input :content, label: false, placeholder: "Message ##{project.title}" %>
        <%= f.submit "Send", class: "btn mt-3 landing-button btn-lg"%>
      <% end %>
    <% else %>
      <%= simple_form_for [project, user_project] do |f| %>
        <%= f.submit "Enroll and join chat!", class: "btn landing-button btn-lg"%>
      <% end %>
    <% end %>
  </div>
</div>
